#Использовать "./Модули"

Если НЕ АргументыКоманднойСтроки.Количество() = 0 Тогда
	
	Сообщить("Обрабатываем директорию: " + АргументыКоманднойСтроки[0]);

	ВыводитьДетальныеСообщения = 0;

	Если АргументыКоманднойСтроки.Количество() = 2 Тогда
		ВыводитьДетальныеСообщения = Число(АргументыКоманднойСтроки[1]);	
	КонецЕсли;

	Replace.ОбработатьЭлементыСтилейНаСервере(АргументыКоманднойСтроки[0], ВыводитьДетальныеСообщения);

КонецЕсли;
